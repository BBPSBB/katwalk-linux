# Python deps for katwalk-linux. Create a venv and install:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# The core daemon reads the device over hidraw (stdlib only). Pillow renders the in-VR HUD
# (the OpenXR layer displays it - no other VR packages needed). pyusb wakes the device.
# Versions below are what was tested; newer ones will likely work too.
pyusb==1.3.1
Pillow==12.2.0
